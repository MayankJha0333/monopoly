services:
  rentrush:
    build: .
    ports:
      - "3001:3001"
    environment:
      NODE_ENV: production
      TRUST_PROXY: "1"
      # COOKIE_SECURE: "0"   # only for testing over plain http
    volumes:
      - rentrush-data:/data
    restart: unless-stopped
volumes:
  rentrush-data:
